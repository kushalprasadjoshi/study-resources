# Automatically detect text files and normalize line endings to LF
* text=auto
# Treat Markdown files as text
*.md text

# Track some large files with Git LFS
# Note: You need to have Git LFS installed and initialized in your repository for this to work.
handwritten-notes/basic-data-science.pdf filter=lfs diff=lfs merge=lfs -text
handwritten-notes/basic-data-science-all-chapters.zip filter=lfs diff=lfs merge=lfs -text
